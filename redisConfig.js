// redisConfig.js (example)
export const connection = {
  host: "127.0.0.1",
  port: 6379,
  // password: "your-password",   // if needed
  // username: "default",         // if needed
  // tls: {},                     // if using Redis Cloud/SSL
};
